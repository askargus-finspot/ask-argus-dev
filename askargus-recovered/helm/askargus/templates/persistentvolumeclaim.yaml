{{- if .Values.askargus.imageVolume.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "askargus.fullname" $ }}-images
spec:
  accessModes:
    - {{ .Values.askargus.imageVolume.accessModes }}
{{- if .Values.askargus.imageVolume.storageClassName }}
  storageClassName: {{ .Values.askargus.imageVolume.storageClassName }}
{{- end }}          
  volumeMode: Filesystem
  resources:
    requests:
      storage: {{ .Values.askargus.imageVolume.size }}
{{- end }}
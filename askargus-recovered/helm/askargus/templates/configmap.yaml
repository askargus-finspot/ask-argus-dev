{{- if .Values.askargus.configYamlContent }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "askargus.fullname" $ }}-config
data:
  askargus.yaml: |
{{ .Values.askargus.configYamlContent | toYaml | indent 4 }}

{{- end }}
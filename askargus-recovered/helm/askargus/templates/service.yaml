apiVersion: v1
kind: Service
metadata:
  name: {{ include "askargus.fullname" . }}
  labels:
    {{- include "askargus.labels" . | nindent 4 }}
  annotations:
  {{- toYaml .Values.service.annotations | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "askargus.selectorLabels" . | nindent 4 }}
